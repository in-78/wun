<% if item.complete %>
  Description:
  <%= item.description.html_safe %>
<% else %>
  <div class="field">
    <%= f.text_area :description, class: "tinymce", rows: 15 %>
  </div>
  <%= tinymce %>
<% end %>