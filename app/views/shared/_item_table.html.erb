<table id="items" data-update-url="<%= sort_items_url %>" class="table table-striped">
<% items.each do |item| %>
	<%= content_tag_for :tr, item do %>
    <td><%= item.complete ? 'Done' : 'Todo' %></td>
    <td><%= link_to item.name, edit_item_path(item) %></td>
    <td><%= item.date_due %></td>
    <td><%= 'Star' if item.star %></td>
  <% end %>
<% end %>
</table>

<%= paginate items %>